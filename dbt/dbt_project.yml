name: 'global_macro_analysis'
version: '1.0.0'
config-version: 2

profile: 'global_macro_analysis'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  global_macro_analysis:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
